#!/usr/bin/make -f

# exclude svn dirs from packages
DEB_DH_ALWAYS_EXCLUDE=.svn
DEB_DH_MD5SUMS_ARGS=-X/.svn

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/python-distutils.mk

pre-build::
	make -C doc

clean::
	make -C doc clean
